Pre-requirements:
=================

For the matrix calculations you will need a decent C++ compiler and
blitz++ (Debian package or from http://oonumerics.org/blitz/)

Installation:
=============

libnuosc++ is now built using GNU autotools.

Type:

  ./configure
  make
  make install

You can see what build options are available via

  ./configure --help


